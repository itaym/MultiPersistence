# BASE is a BigInt value in the range 3n to 16384n
BASE=10n
# ------------------------------------------------
# GOAL_POWER_OF10 is an Integer value in the range
# 0 to aprox. 8k,  NOTE: As the BASE is higher the
# GOAL_POWER_OF10 is lower. Map size error occures
GOAL_POWER_OF10="499"
# ------------------------------------------------
# LAST_NUMBER is for debug purposes.
LAST_NUMBER="(()=>{let r = normalizedEnv.base; return (new HugeInt().fromString('' + (r ** 0n - 1n).toString(r).split('').sort().join(''), r)).value})()"
# ------------------------------------------------
# LOG_INTERVAL is an Integer for how many millisec
# to show the log in terminal and save VARS_FILE
LOG_INTERVAL=2_000
# ------------------------------------------------
# VARS_FILE The name of the result file generated.
# The format is <BASE 5 Chars>VARS_FILE
# exmpl.: 000010initVars.json / 000010initVars.bak
VARS_FILE='initVars.json'
# ------------------------------------------------
# MEMORIZE_CACHE_DIR memorize use cache directory.
# files are read from / written to.
MEMORIZE_CACHE_DIR='./caching'
# ------------------------------------------------
# MEMORIZE_SAVE_BACH How many changes to a chached
# Object before saving it.
MEMORIZE_SAVE_BACH=20
# ------------------------------------------------
# DEBUG boolean (true/false) In debug mode no
# VARS_FILE will be read / write
DEBUG=true
# ------------------------------------------------